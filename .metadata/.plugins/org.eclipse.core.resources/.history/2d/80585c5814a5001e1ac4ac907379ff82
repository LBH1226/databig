package 변수및자료형;

public class EX03변수명 {

	public static void main(String[] args) {

		// 변수명 사용 규칙
		// 1. 키워드는 사용 불가 (키워드는 다른색으로 변함)
		// 2. 대소문자가 구분되며 길이에 제한이 없다
		//    --> 변수명은 의미전달이 중요 ex) a,b,c,d와 같이 의미없이 짓지 말기
		// 3. 숫자로 시작할 수 없다 (숫자는 값으로 인식)
		// 4. 특수문자는 _ 와 $ 만 사용 가능
		
		// 자바 개발자에게 권장되는 규칙들
		// 1. 변수 시작은 소문자로
		// 2. 여러글자로 이어지는 경우에는 이어지는 단어의 첫글자를 대문자로(카멜식기법)
		//    ex) int inputNumber = 4;
		// 3. 
		
		
		
		
	}

}
